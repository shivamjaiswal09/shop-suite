{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // `DOM` here only supplies the universal timer globals (setTimeout) used to
    // simulate latency — no browser API is referenced.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src/**/*.ts"]
}
